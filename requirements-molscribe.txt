# MolScribe venv only
# Colleague fork; if you have a newer private fork that fixes albumentations 2.x, swap the git URL.
git+https://github.com/breadbread1984/MolScribe.git

# CRITICAL: MolScribe (and many forks) still call A.augmentations.pad_with_params
# and DualTransform.apply_to_keypoint. albumentations>=1.4.18 / 2.x break these
# → every image prediction fails, job still finishes as done, markdown keeps ![](images/...).
# Do NOT remove this pin unless your MolScribe fork is verified on the same albumentations.
albumentations==1.3.1

torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
huggingface_hub>=0.20.0
tqdm>=4.65.0
