# MinerU - PDF document parsing (pipeline backend; no vLLM required)
mineru[all]>=3.0.0

# Optional: vLLM for VLM/hybrid backends (Linux+CUDA recommended; skip on Windows)
# vllm>=0.7.0

# MolScribe - chemical structure recognition (image -> SMILES)
# Modified fork for enhanced compatibility
git+https://github.com/breadbread1984/MolScribe.git
# MolScribe still uses DualTransform.apply_to_keypoint (singular) and
# A.augmentations.pad_with_params; albumentations>=1.4.18 / 2.x break these.
albumentations==1.3.1

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
huggingface_hub>=0.20.0

# Environment variable loading
python-dotenv>=1.0.0

# Utility
tqdm>=4.65.0
